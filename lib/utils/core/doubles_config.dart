import 'package:flutter/material.dart';

const EdgeInsets kPadding = EdgeInsets.all(16.0);
const EdgeInsets kSmallPadding = EdgeInsets.all(4.0);
BorderRadius kBRadius = BorderRadius.circular(12);
const Radius kRadius = Radius.circular(12);
const double kSpacing = 16.0;
const double kHalfSpace = 8.0;
